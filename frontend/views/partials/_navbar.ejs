<nav
  class="w-full bg-white/10 glass rounded-2xl p-4 md:p-6 border border-white/20 mb-8"
>
  <div
    class="flex flex-col md:flex-row md:justify-between md:items-center gap-4 md:gap-0"
  >
    <div
      class="flex flex-col md:flex-row md:items-center gap-2 md:gap-4 w-full md:w-auto"
    >
      <div class="flex items-center gap-3">
        <div
          class="w-8 h-8 md:w-10 md:h-10 bg-gradient-to-r from-pink-500 to-purple-600 rounded-xl flex items-center justify-center"
        >
          <svg
            class="w-6 h-6 text-white"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"
            ></path>
          </svg>
        </div>
        <h1
          class="flex items-center text-lg md:text-2xl font-bold text-white pl-2 "
        >
          TaskProse
        </h1> 
      </div>

      <div
        class="relative w-full md:w-auto md:ml-4 mt-2 flex items-center"
      >
        <input
          id="search-input"
          type="text"
          class="h-8 md:h-10 w-full md:w-20 focus:md:w-44 px-3 bg-gray-900 border border-white/20 rounded-lg text-white hover:border-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-600 transition-all delay-100 duration-300 md:transition-width"
          placeholder="Ara"
        />
        <button
          id="icon-clear"
          class="absolute hidden text-gray-200 hover:text-white right-2 top-1/2 -translate-y-1/2 rounded-full hover:bg-white/20"
          title="Temizle"
        >
          <i class="bi bi-x-lg"></i>
        </button>
      </div>
    </div>

    <div
      class="flex items-center gap-3 w-full md:w-auto justify-between md:justify-end mt-3 md:mt-0"
    >
      <div class="text-right">
        <p class="text-white font-semibold">
          <% if (typeof user !== 'undefined' && user) { %> <%= user.first_name
          %> <%= user.last_name %> <% } else { %> Kullanıcı <% } %>
        </p>
        <p class="text-white/70 text-sm">
          <% if (typeof user !== 'undefined' && user) { %> <%= user.email %> <%
          } else { %> Giriş yapılmamış <% } %>
        </p>
      </div>
      <div
        class="w-12 h-10 md:h-12 bg-gradient-to-r from-blue-400 to-purple-500 rounded-full flex items-center justify-center text-white font-bold text-lg"
      >
        <% if (typeof user !== 'undefined' && user) { %> <%=
        user.first_name.charAt(0) %><%= user.last_name.charAt(0) %> <% } else {
        %> K <% } %>
      </div>
      <form id="logoutForm" style="display: inline">
        <button
          type="submit"
          class="py-1 px-1 md:py-2 md:px-3 text-white/70 bg-gradient-to-r from-red-600 to-pink-600 hover:from-red-500 hover:to-pink-500 hover:scale-105 rounded-lg transition-all duration-200"
        >
          Çıkış Yap
        </button>
      </form>
    </div>
  </div>
</nav>
<script src="/public/js/components/search-bar.js"></script>
